let sessions=[];
let activeSessionId=Number(localStorage.getItem('activeSessionId')||0);
let currentReport='daily';
let historyFilter='all';
let quickMode='entry';
let dashboardMode=localStorage.getItem('dashboardMode')||'pass';

const $=id=>document.getElementById(id);
const num=v=>Number(String(v??'').replace(',','.'))||0;
const brl=v=>num(v).toLocaleString('pt-BR',{style:'currency',currency:'BRL'});
const pct=v=>num(v).toFixed(1).replace('.',',')+'%';
const km=v=>num(v).toFixed(1).replace('.',',')+' km';
const od=v=>num(v)>0?num(v).toLocaleString('pt-BR',{maximumFractionDigits:1}):'-';

function nowLocal(){const d=new Date(),p=x=>String(x).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`}
function dateKey(d){const p=x=>String(x).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}`}
function monthKey(d){const p=x=>String(x).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}`}
function fmtDate(v){if(!v)return'-';const[d,t='']=String(v).split('T');const[y,m,day]=d.split('-');return `${day}/${m}/${y} ${t}`}
function fmtShortDate(v){if(!v)return'-';const [y,m,d]=String(v).slice(0,10).split('-');return `${d}/${m}`}
function endDate(s){return new Date(s.ended_at)}
function isExpired(s){return Date.now()>endDate(s).getTime()}
function isActive(s){return s&&!isExpired(s)}
function openSession(){return sessions.find(s=>isActive(s))||null}
function leftTime(s){const ms=endDate(s)-new Date();if(ms<=0)return'Expirado';const h=Math.floor(ms/3600000),m=Math.floor((ms%3600000)/60000),d=Math.floor(h/24),rh=h%24;return d>0?`${d}d ${rh}h ${m}min`:`${h}h ${m}min`}
function showError(msg){const box=$('globalError');if(!box)return;box.style.display='block';box.textContent=msg}
function clearError(){const box=$('globalError');if(!box)return;box.style.display='none';box.textContent=''}
function yesWallet(v){return v!==false&&v!=='false'}
function moneyClass(v){return num(v)>=0?'good':'bad'}
function changeText(cur,prev){const p=prev?((cur-prev)/Math.abs(prev))*100:cur?100:0;return `${p>=0?'+':''}${pct(p)}`}
function changeClass(cur,prev){return num(cur)>=num(prev)?'good':'bad'}

async function api(path,opt={}){const headers=opt.headers||{};headers['Content-Type']='application/json';const pin=localStorage.getItem('appPin')||'';if(pin)headers['x-app-pin']=pin;const res=await fetch(path,{...opt,headers});if(res.status===401){showLogin();throw new Error('PIN inválido')}const data=(res.headers.get('content-type')||'').includes('json')?await res.json():await res.text();if(!res.ok)throw new Error(data.error||'Erro');return data}
function showLogin(){$('loginBox').style.display='flex'}
function hideLogin(){$('loginBox').style.display='none'}
async function login(){try{const pin=$('pinInput').value.trim();await api('/api/login',{method:'POST',body:JSON.stringify({pin})});localStorage.setItem('appPin',pin);hideLogin();loadData()}catch(e){$('loginError').style.display='block';$('loginError').textContent=e.message}}
function logout(){localStorage.removeItem('appPin');showLogin()}
function showTab(name){document.querySelectorAll('.section').forEach(e=>e.classList.remove('active'));document.querySelectorAll('.tab').forEach(e=>e.classList.remove('active'));$(name).classList.add('active');const tab=document.querySelector(`[data-tab="${name}"]`);if(tab)tab.classList.add('active');if(name==='historico')renderHistory();if(name==='relatorios')loadReport(currentReport);if(name==='novo')renderNewWarning();if(name==='dashboard')renderDashboard()}
function showDashMode(mode){dashboardMode=mode;localStorage.setItem('dashboardMode',mode);renderDashboard()}

async function loadData(){clearError();try{sessions=await api('/api/sessions');const open=openSession();if(open)activeSessionId=open.id;if(activeSessionId&&!sessions.find(s=>s.id===activeSessionId)){activeSessionId=0;localStorage.removeItem('activeSessionId')}if(activeSessionId)localStorage.setItem('activeSessionId',activeSessionId);renderDashboard();renderHistory();renderNewWarning();loadReport(currentReport,true)}catch(e){showError(e.message)}}
function renderNewWarning(){const open=openSession(),box=$('activeWarning');if(!box)return;if(open){box.style.display='block';box.innerHTML=`Já existe um passe vigente: <b>${open.pass_type}h</b>, iniciado em <b>${fmtDate(open.started_at)}</b>. Tempo restante: <b>${leftTime(open)}</b>. Para iniciar outro, cancele/exclua o atual.`}else box.style.display='none'}
function card(label,value,cls=''){return `<div class="card"><div class="label">${label}</div><div class="value ${cls}">${value}</div></div>`}
function rowItem(title,sub='',cls=''){return `<div class="smart-item"><b class="${cls}">${title}</b><span>${sub}</span></div>`}
function renderList(id,html,empty='Sem dados ainda.'){const box=$(id);if(!box)return;box.innerHTML=html||`<p class="hint">${empty}</p>`}
function updateGaugeMeta(x){const chart=$('gaugeChart');const panel=chart?.closest('.gauge-panel');if(!panel)return;let meta=$('gaugeMeta');if(!meta){meta=document.createElement('div');meta.id='gaugeMeta';meta.className='gauge-meta';chart.insertAdjacentElement('afterend',meta)}meta.innerHTML=`<div class="mini-card"><span class="mini-label">Meta saudável</span><span class="mini-value good">abaixo de 15%</span></div><div class="mini-card"><span class="mini-label">Falta p/ 15%</span><span class="mini-value ${x.missing_15>0?'warning':'good'}">${brl(x.missing_15)}</span></div>`}

function updateDashVisibility(){
  document.querySelectorAll('.smart-tab').forEach(b=>b.classList.toggle('active',b.dataset.dash===dashboardMode));
  document.querySelectorAll('.dash-view').forEach(v=>v.classList.remove('active'));
  const view=$(`${dashboardMode}Dash`);if(view)view.classList.add('active');
}

function renderDashboard(){
  updateDashVisibility();
  let s=sessions.find(x=>x.id===activeSessionId);
  if(s&&isExpired(s)){activeSessionId=0;s=null}
  const hasPass=!!s;
  if($('activeWrap'))$('activeWrap').style.display='block';
  if($('emptyActive'))$('emptyActive').style.display=!hasPass&&dashboardMode==='pass'?'block':'none';
  const modeTitle={pass:'Passe ativo',day:'Dia',week:'Semana',month:'Mês'}[dashboardMode]||'Dashboard';
  if($('executiveBox'))$('executiveBox').textContent=hasPass&&dashboardMode==='pass'?(s.summary.executive_message||'Dia no controle.'):`Dashboard ${modeTitle}: analisando seu histórico de corridas, KM e despesas.`;
  if(hasPass)renderPassDashboard(s);
  renderSmartDashboards();
}

function renderPassDashboard(s){
  const x=s.summary;
  $('percentText').textContent=pct(x.pass_percent);
  $('statusText').textContent=x.status.label;
  $('statusText').className='status '+x.status.key;
  $('barFill').style.width=Math.min(x.pass_percent,100)+'%';
  $('barFill').style.background=x.status.key==='good'?'var(--green)':x.status.key==='bad'?'var(--red)':'var(--yellow)';
  updateGaugeMeta(x);
  $('financeCards').innerHTML=card('Bruto total',brl(x.total_gross),'info')+card('Despesas totais',brl(x.total_expenses),'bad')+card('Líquido real',brl(x.net_real),x.net_real>=0?'good':'bad')+card('Margem líquida',pct(x.margin_real_percent),x.margin_real_percent>=60?'good':'warning')+card('Líquido sem km',brl(x.net_simple))+card('Líquido com km',brl(x.net_with_km))+card('Outras despesas',brl(x.extra_expenses),'bad')+card('Líquido por hora',brl(x.net_per_hour),x.net_per_hour>=25?'good':'warning');
  renderWalletCards(s);
  $('operationCards').innerHTML=card('Corridas',x.total_rides)+card('KM rodado',km(x.total_km))+card('KM/corrida',km(x.average_km_per_ride))+card('Média/corrida',brl(x.average_per_ride))+card('Bruto por km',brl(x.gross_per_km),'info')+card('Líquido por km',brl(x.net_per_km),x.net_per_km>=1?'good':'warning')+card('Bruto por hora',brl(x.gross_per_hour),'info')+card('Corridas por hora',x.rides_per_hour);
  $('costCards').innerHTML=card('KM inicial',od(x.start_odometer))+card('KM vindo das corridas',km(x.total_km),'info')+card('Custo km',brl(x.km_cost),'bad')+card('Custo total %',pct(x.total_cost_percent),x.total_cost_percent<35?'good':'warning')+card('Passe',brl(s.pass_price))+card('Passe por corrida',brl(x.total_rides?num(s.pass_price)/x.total_rides:0))+card('Custo por corrida',brl(x.cost_per_ride),'warning')+card('Falta p/ 15%',brl(x.missing_15));
  $('activeInfo').innerHTML=`<span class="badge good">VIGENTE</span> ${s.platform} • Passe ${s.pass_type}h de <b>${brl(s.pass_price)}</b> • início <b>${fmtDate(s.started_at)}</b> • fim <b>${fmtDate(s.ended_at)}</b> • tempo restante <b>${leftTime(s)}</b> • custo/km <b>${brl(s.cost_per_km)}</b>`;
  renderEntries(s);renderExpenses(s);drawAllCharts(s);
}
function renderWalletCards(s){const box=$('walletCards');if(!box)return;const x=s.summary;if(!x.wallet_has_value){box.innerHTML=card('Carteira não informada','Atualizar','warning')+card('Líquido real',brl(x.net_real),x.net_real>=0?'good':'bad')+card('Movimento carteira','-', '')+card('Diferença','-', '');return}box.innerHTML=card('Carteira informada',brl(x.wallet_balance),'info')+card('Carteira projetada',brl(x.wallet_projected),x.wallet_projected>=0?'good':'bad')+card('Movimento após ajuste',brl(x.wallet_delta),x.wallet_delta>=0?'good':'bad')+card('Diferença carteira x líquido',brl(x.wallet_gap_vs_net),Math.abs(num(x.wallet_gap_vs_net))<=5?'good':'warning')+card('Entradas na carteira',brl(x.wallet_entries),'info')+card('Saídas da carteira',brl(x.wallet_expenses),'bad')+card('Atualizado em',fmtDate(x.wallet_updated_at),'')+card('Líquido real',brl(x.net_real),x.net_real>=0?'good':'bad')}
function renderEntries(s){const list=$('entriesList');if(!list)return;if(!s.entries?.length){list.innerHTML='<p class="hint">Nenhum ganho lançado.</p>';return}list.innerHTML=s.entries.map(e=>`<div class="item"><div><b>${brl(e.amount)}</b><div class="hint">${fmtDate(e.created_at)} • ${e.rides_count} corrida(s) • trecho ${km(e.km_delta)} • ${yesWallet(e.affects_wallet)?'somou na carteira':'não somou na carteira'}</div><div class="hint">${e.note||''}</div></div><button class="danger-btn" onclick="deleteEntry(${e.id})">Excluir</button></div>`).join('')}
function renderExpenses(s){const list=$('expensesList');if(!list)return;if(!s.expenses?.length){list.innerHTML='<p class="hint">Nenhuma despesa lançada.</p>';return}list.innerHTML=s.expenses.map(e=>`<div class="item"><div><b>${e.category}: ${brl(e.amount)}</b><div class="hint">${fmtDate(e.created_at)} • ${yesWallet(e.affects_wallet)?'descontou da carteira':'só no lucro, não descontou da carteira'}</div><div class="hint">${e.note||''}</div></div><button class="danger-btn" onclick="deleteExpense(${e.id})">Excluir</button></div>`).join('')}
async function createSession(){const open=openSession();if(open){showError('Já existe um passe vigente. Exclua/cancele o atual para criar outro.');showTab('dashboard');return}try{const body={platform:$('newPlatform').value,pass_type:Number($('newType').value),pass_price:Number($('newPrice').value),cost_per_km:Number($('newKmCost').value),start_odometer:Number($('newStartOdometer').value||0),started_at:$('newStart').value||nowLocal(),notes:$('newNotes').value};const created=await api('/api/sessions',{method:'POST',body:JSON.stringify(body)});activeSessionId=created.id;localStorage.setItem('activeSessionId',activeSessionId);await loadData();showTab('dashboard')}catch(e){showError(e.message)}}

function flatEntries(){return sessions.flatMap(s=>(s.entries||[]).map(e=>({...e,session_id:s.id,session_started_at:s.started_at,cost_per_km:num(s.cost_per_km,.81),date:String(e.created_at||'').slice(0,10),hour:Number(String(e.created_at||'T00').split('T')[1]?.slice(0,2)||0)})))}
function flatExpenses(){return sessions.flatMap(s=>(s.expenses||[]).map(e=>({...e,session_id:s.id,cost_per_km:num(s.cost_per_km,.81),date:String(e.created_at||'').slice(0,10),hour:Number(String(e.created_at||'T00').split('T')[1]?.slice(0,2)||0)})))}
function startOfDay(d){const x=new Date(d);x.setHours(0,0,0,0);return x}
function addDays(d,n){const x=new Date(d);x.setDate(x.getDate()+n);return x}
function startOfWeek(d){const x=startOfDay(d);const day=(x.getDay()+6)%7;x.setDate(x.getDate()-day);return x}
function startOfMonth(d){const x=startOfDay(d);x.setDate(1);return x}
function addMonths(d,n){const x=new Date(d);x.setMonth(x.getMonth()+n);return x}
function rangeKeys(start,end){return{start:dateKey(start)+'T00:00',end:dateKey(end)+'T00:00',startDay:dateKey(start),endDay:dateKey(end)}}
function inRange(row,r){const c=String(row.created_at||'');return c>=r.start&&c<r.end}
function metrics(start,end){const rge=rangeKeys(start,end),es=flatEntries().filter(e=>inRange(e,rge)),xs=flatExpenses().filter(e=>inRange(e,rge));const gross=es.reduce((a,e)=>a+num(e.amount),0),rides=es.reduce((a,e)=>a+num(e.rides_count),0),kmt=es.reduce((a,e)=>a+num(e.km_delta),0),kmCost=es.reduce((a,e)=>a+num(e.km_delta)*num(e.cost_per_km,.81),0),extra=xs.reduce((a,e)=>a+num(e.amount),0),net=gross-kmCost-extra;const hours=new Set(es.map(e=>e.date+'-'+e.hour));return{entries:es,expenses:xs,gross:num(gross),rides:num(rides),km:num(kmt),kmCost:num(kmCost),extra:num(extra),net:num(net),activeHours:hours.size||0,grossPerKm:kmt?gross/kmt:0,netPerKm:kmt?net/kmt:0,avgRide:rides?gross/rides:0,perActiveHour:hours.size?gross/hours.size:0,range:rge}}
function byHour(m){const arr=Array.from({length:24},(_,h)=>({hour:h,gross:0,rides:0,km:0,count:0}));m.entries.forEach(e=>{const h=Math.max(0,Math.min(23,e.hour||0));arr[h].gross+=num(e.amount);arr[h].rides+=num(e.rides_count);arr[h].km+=num(e.km_delta);arr[h].count++});return arr}
function byDay(start,days){return Array.from({length:days},(_,i)=>{const d=addDays(start,i),m=metrics(d,addDays(d,1));return{date:dateKey(d),label:fmtShortDate(dateKey(d)),...m}})}
function historicalHours(weekday=null){const es=flatEntries().filter(e=>weekday===null||new Date(e.date+'T00:00').getDay()===weekday);const arr=Array.from({length:24},(_,h)=>({hour:h,gross:0,rides:0,km:0,count:0}));es.forEach(e=>{const h=Math.max(0,Math.min(23,e.hour||0));arr[h].gross+=num(e.amount);arr[h].rides+=num(e.rides_count);arr[h].km+=num(e.km_delta);arr[h].count++});return arr}
function topHours(arr,limit=5,minGross=0){return [...arr].filter(x=>x.gross>minGross).sort((a,b)=>b.gross-a.gross).slice(0,limit)}
function scoreFrom(m){if(!m.gross)return'-';const margin=m.gross?m.net/m.gross*100:0;if(margin>=70)return'Excelente';if(margin>=55)return'Bom';if(margin>=35)return'Atenção';return'Pesado'}
function insightLines(cur,prev,label){const lines=[];if(!cur.gross)lines.push(`Ainda sem ganhos no ${label}.`);else lines.push(`Bruto de ${brl(cur.gross)} e líquido estimado de ${brl(cur.net)}.`);if(prev.gross)lines.push(`Comparado ao período anterior: ${changeText(cur.gross,prev.gross)} no bruto e ${changeText(cur.net,prev.net)} no líquido.`);if(cur.km)lines.push(`Rendimento líquido por km: ${brl(cur.netPerKm)}. Bruto por km: ${brl(cur.grossPerKm)}.`);if(cur.activeHours)lines.push(`Média por hora com corrida: ${brl(cur.perActiveHour)}.`);return lines.map(x=>rowItem(x)).join('')}
function renderSmartDashboards(){renderDayDash();renderWeekDash();renderMonthDash()}
function renderDayDash(){const today=startOfDay(new Date()),yesterday=addDays(today,-1),tomorrow=addDays(today,1),m=metrics(today,tomorrow),p=metrics(yesterday,today);$('dayScore').textContent=scoreFrom(m);$('daySummaryText').textContent=p.gross?`Hoje está ${changeText(m.gross,p.gross)} em bruto contra ontem.`:'Sem base de ontem suficiente para comparação.';$('dayCards').innerHTML=card('Bruto hoje',brl(m.gross),'info')+card('Líquido estimado',brl(m.net),moneyClass(m.net))+card('Corridas',m.rides)+card('KM',km(m.km))+card('Hoje x ontem',changeText(m.gross,p.gross),changeClass(m.gross,p.gross))+card('R$/km líquido',brl(m.netPerKm),m.netPerKm>=1?'good':'warning')+card('Média/corrida',brl(m.avgRide),'info')+card('Despesa + KM',brl(m.extra+m.kmCost),'bad');const hours=byHour(m);drawBarSeries('dayHourlyChart',hours.map(x=>`${x.hour}h`),hours.map(x=>x.gross),'Bruto por hora');drawCompareBars('dayCompareChart',['Bruto','Líquido','Corridas','KM'],[m.gross,m.net,m.rides,m.km],[p.gross,p.net,p.rides,p.km]);renderList('dayBestHours',topHours(hours,5).map(h=>rowItem(`${String(h.hour).padStart(2,'0')}h`,`${brl(h.gross)} • ${h.rides} corrida(s) • ${km(h.km)}`,'good')).join(''),'Sem corridas hoje.');const hist=topHours(historicalHours(new Date().getDay()),6).map(h=>rowItem(`${String(h.hour).padStart(2,'0')}h`,`${brl(h.gross)} no histórico deste dia da semana • ${h.rides} corrida(s)`,'info')).join('');renderList('dayPeakForecast',hist,'Ainda não há histórico suficiente para prever pico.');renderList('dayInsights',insightLines(m,p,'dia'))}
function renderWeekDash(){const start=startOfWeek(new Date()),end=addDays(start,7),prevStart=addDays(start,-7),m=metrics(start,end),p=metrics(prevStart,start);$('weekScore').textContent=scoreFrom(m);$('weekSummaryText').textContent=p.gross?`Semana atual está ${changeText(m.gross,p.gross)} contra a semana passada.`:'A semana passada ainda não tem base suficiente.';$('weekCards').innerHTML=card('Bruto semana',brl(m.gross),'info')+card('Líquido semana',brl(m.net),moneyClass(m.net))+card('Corridas',m.rides)+card('KM',km(m.km))+card('Semana x passada',changeText(m.gross,p.gross),changeClass(m.gross,p.gross))+card('R$/km líquido',brl(m.netPerKm),m.netPerKm>=1?'good':'warning')+card('Média/corrida',brl(m.avgRide),'info')+card('Despesa + KM',brl(m.extra+m.kmCost),'bad');const days=byDay(start,7);drawBarSeries('weekDaysChart',days.map(d=>d.label),days.map(d=>d.gross),'Bruto por dia');drawCompareBars('weekCompareChart',['Bruto','Líquido','Corridas','KM'],[m.gross,m.net,m.rides,m.km],[p.gross,p.net,p.rides,p.km]);renderList('weekBestDays',[...days].sort((a,b)=>b.gross-a.gross).filter(d=>d.gross>0).slice(0,5).map(d=>rowItem(d.label,`${brl(d.gross)} • líquido ${brl(d.net)} • ${d.rides} corrida(s)`,'good')).join(''),'Sem corridas nesta semana.');renderList('weekBestHours',topHours(byHour(m),5).map(h=>rowItem(`${String(h.hour).padStart(2,'0')}h`,`${brl(h.gross)} • ${h.rides} corrida(s)`,'info')).join(''),'Sem horário forte nesta semana.');renderList('weekInsights',insightLines(m,p,'semana'))}
function renderMonthDash(){const start=startOfMonth(new Date()),end=addMonths(start,1),prevStart=addMonths(start,-1),m=metrics(start,end),p=metrics(prevStart,start);$('monthScore').textContent=scoreFrom(m);$('monthSummaryText').textContent=p.gross?`Mês atual está ${changeText(m.gross,p.gross)} contra o mês passado.`:'O mês passado ainda não tem base suficiente.';$('monthCards').innerHTML=card('Bruto mês',brl(m.gross),'info')+card('Líquido mês',brl(m.net),moneyClass(m.net))+card('Corridas',m.rides)+card('KM',km(m.km))+card('Mês x passado',changeText(m.gross,p.gross),changeClass(m.gross,p.gross))+card('R$/km líquido',brl(m.netPerKm),m.netPerKm>=1?'good':'warning')+card('Média/corrida',brl(m.avgRide),'info')+card('Despesa + KM',brl(m.extra+m.kmCost),'bad');const daysCount=Math.round((end-start)/86400000),days=byDay(start,daysCount);drawBarSeries('monthDaysChart',days.map(d=>d.label),days.map(d=>d.gross),'Bruto por dia');drawCompareBars('monthCompareChart',['Bruto','Líquido','Corridas','KM'],[m.gross,m.net,m.rides,m.km],[p.gross,p.net,p.rides,p.km]);renderList('monthBestDays',[...days].sort((a,b)=>b.gross-a.gross).filter(d=>d.gross>0).slice(0,7).map(d=>rowItem(d.label,`${brl(d.gross)} • líquido ${brl(d.net)} • ${d.rides} corrida(s)`,'good')).join(''),'Sem corridas neste mês.');renderList('monthBestHours',topHours(byHour(m),6).map(h=>rowItem(`${String(h.hour).padStart(2,'0')}h`,`${brl(h.gross)} • ${h.rides} corrida(s)`,'info')).join(''),'Sem horário forte neste mês.');renderList('monthInsights',insightLines(m,p,'mês'))}

function openQuickModal(mode='entry'){const s=sessions.find(x=>x.id===activeSessionId)||openSession();if(!s){showError('Crie ou abra um passe vigente antes de lançar entrada/despesa.');showTab('dashboard');return}activeSessionId=s.id;localStorage.setItem('activeSessionId',activeSessionId);setQuickMode(mode);const m=$('quickModal');if(m){m.classList.add('show');m.setAttribute('aria-hidden','false')}setQuickDefaults()}
function closeQuickModal(){const m=$('quickModal');if(m){m.classList.remove('show');m.setAttribute('aria-hidden','true')}}
function modalBackdropClick(e){if(e.target?.id==='quickModal')closeQuickModal()}
function setQuickMode(mode){quickMode=mode==='expense'?'expense':mode==='wallet'?'wallet':'entry';document.querySelectorAll('.mode-btn').forEach(b=>b.classList.toggle('active',b.dataset.mode===quickMode));const entry=$('quickEntryForm'),expense=$('quickExpenseForm'),wallet=$('quickWalletForm');if(entry)entry.style.display=quickMode==='entry'?'block':'none';if(expense)expense.style.display=quickMode==='expense'?'block':'none';if(wallet)wallet.style.display=quickMode==='wallet'?'block':'none';const title=$('quickModalTitle'),hint=$('quickModalHint');if(title)title.textContent=quickMode==='entry'?'Adicionar entrada':quickMode==='expense'?'Adicionar despesa':'Atualizar carteira';if(hint)hint.textContent=quickMode==='entry'?'Entrada manual sem KM. O KM das corridas vem do MacroDroid.':quickMode==='expense'?'Escolha se a despesa saiu da carteira ou se é só ajuste de lucro.':'Informe quanto você tem na carteira agora para recalibrar o controle.'}
function setQuickDefaults(){if($('entryTime')&&!$('entryTime').value)$('entryTime').value=nowLocal();if($('expenseTime')&&!$('expenseTime').value)$('expenseTime').value=nowLocal();if($('walletTime'))$('walletTime').value=nowLocal();if(quickMode==='wallet'){const s=sessions.find(x=>x.id===activeSessionId);if(s&&$('walletAmount'))$('walletAmount').value=s.summary?.wallet_has_value?num(s.summary.wallet_projected).toFixed(2):''}setTimeout(()=>{const target=quickMode==='entry'?'entryAmount':quickMode==='expense'?'expenseAmount':'walletAmount';$(target)?.focus()},80)}
async function addEntry(){try{const body={amount:Number($('entryAmount').value),rides_count:Number($('entryRides').value||1),current_odometer:0,affects_wallet:$('entryAffectsWallet')?.checked!==false,created_at:$('entryTime').value||nowLocal(),note:$('entryNote').value};await api(`/api/sessions/${activeSessionId}/entries`,{method:'POST',body:JSON.stringify(body)});$('entryAmount').value='';$('entryRides').value='1';$('entryTime').value=nowLocal();$('entryNote').value='';closeQuickModal();loadData()}catch(e){showError(e.message)}}
async function addExpense(){try{const body={category:$('expenseCategory').value,amount:Number($('expenseAmount').value),affects_wallet:$('expenseAffectsWallet')?.checked!==false,created_at:$('expenseTime').value||nowLocal(),note:$('expenseNote').value};await api(`/api/sessions/${activeSessionId}/expenses`,{method:'POST',body:JSON.stringify(body)});$('expenseAmount').value='';$('expenseTime').value=nowLocal();$('expenseNote').value='';if($('expenseAffectsWallet'))$('expenseAffectsWallet').checked=true;closeQuickModal();loadData()}catch(e){showError(e.message)}}
async function saveWallet(){try{const amount=Number($('walletAmount').value);if(!Number.isFinite(amount)||amount<0)throw new Error('Informe um valor válido para a carteira');await api(`/api/sessions/${activeSessionId}/wallet`,{method:'POST',body:JSON.stringify({wallet_balance:amount,wallet_updated_at:$('walletTime').value||nowLocal()})});closeQuickModal();loadData()}catch(e){showError(e.message)}}
async function deleteEntry(id){if(confirm('Excluir lançamento?')){await api(`/api/entries/${id}`,{method:'DELETE'});loadData()}}
async function deleteExpense(id){if(confirm('Excluir despesa?')){await api(`/api/expenses/${id}`,{method:'DELETE'});loadData()}}
async function deleteSession(id){if(confirm('Excluir passe inteiro?')){await api(`/api/sessions/${id}`,{method:'DELETE'});if(activeSessionId===id)activeSessionId=0;loadData()}}
function openHistorySession(id){activeSessionId=id;localStorage.setItem('activeSessionId',id);dashboardMode='pass';localStorage.setItem('dashboardMode','pass');renderDashboard();showTab('dashboard')}
function filteredSessions(){const today=new Date().toISOString().slice(0,10),month=today.slice(0,7),cut=Date.now()-7*86400000;return sessions.filter(s=>historyFilter==='today'?s.started_at.slice(0,10)===today:historyFilter==='month'?s.started_at.slice(0,7)===month:historyFilter==='7'?new Date(s.started_at).getTime()>=cut:true)}
function renderHistory(){const body=$('historyBody');if(!body)return;const list=filteredSessions();body.innerHTML=list.map(s=>{const x=s.summary,expired=isExpired(s);return `<tr><td>${fmtDate(s.started_at)}</td><td class="${expired?'bad':'good'}">${expired?'Expirado':'Vigente'}</td><td>${s.pass_type}h</td><td>${brl(x.total_gross)}</td><td>${km(x.total_km)}</td><td>${x.total_rides}</td><td>${brl(x.total_expenses)}</td><td>${brl(x.net_real)}</td><td>${x.wallet_has_value?brl(x.wallet_projected):'-'}</td><td>${brl(x.net_per_km)}</td><td>${brl(x.net_per_hour)}</td><td>${pct(x.margin_real_percent)}</td><td class="${x.status.key}">${x.status.label}</td><td><button class="ghost" onclick="openHistorySession(${s.id})">Abrir</button> <button class="danger-btn" onclick="deleteSession(${s.id})">Excluir</button></td></tr>`}).join('')||'<tr><td colspan="14">Sem dados.</td></tr>'}
async function loadReport(period='daily',silent=false){currentReport=period;try{const rows=await api(`/api/reports?period=${period}`);$('reportTitle').textContent={daily:'Resumo diário',weekly:'Resumo semanal',monthly:'Resumo mensal'}[period];$('reportBody').innerHTML=rows.map(r=>`<tr><td>${r.period}</td><td>${r.sessions_count}</td><td>${brl(r.total_gross)}</td><td>${km(r.total_km)}</td><td>${r.total_rides}</td><td>${brl(r.total_expenses)}</td><td>${brl(r.total_net_real)}</td><td>${brl(r.net_per_km)}</td><td>${brl(r.net_per_hour)}</td><td>${pct(r.margin_real_percent)}</td><td class="${r.status.key}">${r.status.label}</td></tr>`).join('')||'<tr><td colspan="11">Sem dados.</td></tr>'}catch(e){if(!silent)showError(e.message)}}

function drawAllCharts(s){drawGauge('gaugeChart',s.summary.pass_percent);drawDonut('donutChart',s);drawLine('lineChart',s);drawBars('barChart',s);drawExpenses('expenseChart',s)}
function ctx(id){const c=$(id);if(!c)return null;const dpr=window.devicePixelRatio||1,w=c.clientWidth||360,h=c.height||220;c.width=w*dpr;c.height=h*dpr;const g=c.getContext('2d');g.scale(dpr,dpr);g.clearRect(0,0,w,h);g.font='12px Arial';return{g,w,h}}
function colors(){return['#22c55e','#38bdf8','#f59e0b','#ef4444','#a78bfa','#fb7185','#14b8a6','#eab308']}
function drawGauge(id,value){const c=ctx(id);if(!c)return;const {g,w,h}=c,cx=w/2,cy=h-8,r=Math.min(w/2-22,h-16,116),thickness=Math.max(10,Math.min(15,r*.17));g.lineCap='round';g.lineWidth=thickness;[['#22c55e',0,.15],['#f59e0b',.15,.25],['#ef4444',.25,1]].forEach(([color,a,b])=>{g.strokeStyle=color;g.beginPath();g.arc(cx,cy,r,Math.PI+a*Math.PI,Math.PI+b*Math.PI);g.stroke()});const v=Math.max(0,Math.min(value/100,1)),angle=Math.PI+v*Math.PI;g.strokeStyle='rgba(255,255,255,.92)';g.lineWidth=3;g.beginPath();g.moveTo(cx,cy);g.lineTo(cx+Math.cos(angle)*(r-9),cy+Math.sin(angle)*(r-9));g.stroke();g.fillStyle='#f8fafc';g.beginPath();g.arc(cx,cy,4,0,Math.PI*2);g.fill()}
function drawDonut(id,s){const c=ctx(id);if(!c)return;const {g,w,h}=c,x=s.summary,vals=[x.net_real,x.km_cost,x.extra_expenses,num(s.pass_price)].map(v=>Math.max(v,0)),labels=['Líquido','Custo km','Despesas','Passe'],sum=vals.reduce((a,b)=>a+b,0)||1,cs=colors();let a=-Math.PI/2;const r=Math.min(w,h)/4.2,cx=w/2,cy=h/2;vals.forEach((v,i)=>{const ang=v/sum*2*Math.PI;g.fillStyle=cs[i];g.beginPath();g.moveTo(cx,cy);g.arc(cx,cy,r,a,a+ang);g.closePath();g.fill();a+=ang});g.fillStyle='#111827';g.beginPath();g.arc(cx,cy,r*.55,0,7);g.fill();labels.forEach((l,i)=>{g.fillStyle=cs[i];g.fillRect(12,18+i*24,12,12);g.fillStyle='#f8fafc';g.fillText(`${l}: ${brl(vals[i])}`,30,29+i*24)})}
function drawLine(id,s){const c=ctx(id);if(!c)return;const {g,w,h}=c,entries=[...(s.entries||[])].reverse();if(!entries.length){g.fillStyle='#9ca3af';g.fillText('Sem lançamentos',20,30);return}let acc=0;const pts=entries.map((e,i)=>{acc+=num(e.amount);return{x:30+i*Math.max((w-60)/(entries.length-1||1),1),y:h-30-(acc/(s.summary.total_gross||1))*(h-60)}});g.strokeStyle='#334155';g.beginPath();g.moveTo(30,10);g.lineTo(30,h-30);g.lineTo(w-10,h-30);g.stroke();g.strokeStyle='#38bdf8';g.lineWidth=3;g.beginPath();pts.forEach((p,i)=>i?g.lineTo(p.x,p.y):g.moveTo(p.x,p.y));g.stroke();g.fillStyle='#f8fafc';g.fillText('Bruto acumulado',35,22)}
function drawBars(id,s){const c=ctx(id);if(!c)return;const {g,w,h}=c,entries=[...(s.entries||[])].reverse(),cs=colors();if(!entries.length){g.fillStyle='#9ca3af';g.fillText('Sem lançamentos',20,30);return}const max=Math.max(...entries.map(e=>num(e.amount)),1),bw=(w-50)/entries.length;entries.forEach((e,i)=>{const bh=num(e.amount)/max*(h-45);g.fillStyle=cs[i%cs.length];g.fillRect(30+i*bw,h-25-bh,Math.max(bw-6,4),bh)});g.fillStyle='#f8fafc';g.fillText('Ganho por lançamento',30,18)}
function drawExpenses(id,s){const c=ctx(id);if(!c)return;const {g,w,h}=c,cats=s.summary.expenses_by_category||{},items=Object.entries(cats),cs=colors();if(!items.length){g.fillStyle='#9ca3af';g.fillText('Sem despesas extras',20,30);return}const max=Math.max(...items.map(x=>x[1]),1);items.forEach(([name,val],i)=>{const y=24+i*28,bar=Math.max(0,(w-170)*(val/max));g.fillStyle=cs[i%cs.length];g.fillRect(120,y-12,bar,16);g.fillStyle='#f8fafc';g.fillText(name,10,y);g.fillText(brl(val),125+bar,y)})}
function drawBarSeries(id,labels,values,title=''){const c=ctx(id);if(!c)return;const {g,w,h}=c,cs=colors(),max=Math.max(...values.map(num),1),bw=(w-50)/(values.length||1);g.fillStyle='#f8fafc';g.fillText(title,30,18);values.forEach((v,i)=>{const bh=num(v)/max*(h-55);g.fillStyle=cs[i%cs.length];g.fillRect(30+i*bw,h-28-bh,Math.max(bw-5,3),bh);if(values.length<=12){g.fillStyle='#94a3b8';g.fillText(labels[i],30+i*bw,h-8)}})}
function drawCompareBars(id,labels,cur,prev){const c=ctx(id);if(!c)return;const {g,w,h}=c,max=Math.max(...cur.map(num),...prev.map(num),1),groupW=(w-50)/(labels.length||1);g.fillStyle='#f8fafc';g.fillText('Atual x anterior',30,18);labels.forEach((l,i)=>{const x=30+i*groupW,cv=num(cur[i]),pv=num(prev[i]),ch=cv/max*(h-60),ph=pv/max*(h-60);g.fillStyle='#38bdf8';g.fillRect(x,h-28-ch,Math.max(groupW*.32,8),ch);g.fillStyle='#f59e0b';g.fillRect(x+Math.max(groupW*.36,12),h-28-ph,Math.max(groupW*.32,8),ph);g.fillStyle='#94a3b8';g.fillText(l,x,h-8)});g.fillStyle='#38bdf8';g.fillText('Atual',w-105,18);g.fillStyle='#f59e0b';g.fillText('Anterior',w-60,18)}

async function boot(){if($('newStart'))$('newStart').value=nowLocal();if($('entryTime'))$('entryTime').value=nowLocal();if($('expenseTime'))$('expenseTime').value=nowLocal();if($('walletTime'))$('walletTime').value=nowLocal();try{const h=await fetch('/api/health').then(r=>r.json());if(h.pin_required&&!localStorage.getItem('appPin'))showLogin();await loadData();setInterval(()=>{renderDashboard();renderNewWarning()},60000)}catch(e){showError('Erro ao carregar servidor.')}}
boot();
